{
    "name": "seatbooker",
    "version": "1.0.0",
    "engines": {
        "node": ">=18 <19"
    },
    "scripts": {
        "start": "cd backend && npm run built && node server.ts",
        "prebuild": "cd backend && npm run build",
        "build": "cd frontend && npm run build"
    }
}