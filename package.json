{
    "name": "seatbooker",
    "version": "1.0.0",
    "engines": {
        "node": ">=18 <19"
    },
    "scripts": {
        "start": "cp -r frontend/dist/frontend/* backend/public/ && cd backend/built && node server.js",
        "prebuild": "cd backend && npm run build",
        "build": "cd frontend && npm run build"
    }
}